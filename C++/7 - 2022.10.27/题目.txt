编写一个一维整型数组类，采用动态内存分配方式，仅用int* p和一些必要的变量作为成员变量。通过成员函数计算该数组的最大值，返回并打印输出。

编写一个二维小数数组类，采用动态内存分配方式，采用整体new的方式编写。通过成员函数计算该数组的均值，返回并打印输出。

编写一个二维小数数组类，采用动态内存分配方式，采用分开new的方式编写。通过成员函数计算该数组的方差，返回并打印输出。

重复一遍书本中的ArrayofPoint类，掌握浅拷贝、深拷贝。

实现Mystring类，它类似C++中的string类，请实现Mystring类的构造函数、析构函数、拷贝构造函数及你能想到的成员函数。

