1 使用deque，存储用户输入的N个小数，再依次打印并输出这些小数的均值

2 使用stack，逆序打印输出'a'-'z'

3 放若干书本对象进queue，并顺序输出这些书本的书名、价格

4 将一些无序的小数放入set，并从小到大顺序输出。

5 使用map，按学号，依次输出学生姓名和成绩。

6 使用mutlimap，存放书本信息（编号、书名、出版社、价格）。要求能插入、删除书本，查询某书本信息，统计已卖出的书本总金额等。

7 实现Mydeque（可不实现迭代器和内存分配器），并基于Mydeque实现Mystack和Myqueue。（这题选做）

8 以模板类的方式，实现一个红黑树或AVL树，具备节点插入、删除、查找等基本。并提供相应的迭代器，可不实现内存分配器。（这题选做）

9 把书本中的所有算法，都在vector上使用一遍。（这题不用交）