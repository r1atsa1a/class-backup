1，声明并实现一个Triangle类，有成员变量三条边、周长和面积，成员函数Getarea()， Getlength()。实现重载的构造函数、拷贝构造函数、析构函数，并在main()中测试该类。

2，声明并实现一个Circle类，有成员变量radius（半径）、成员函数Getarea()。（要求实现重载的构造函数、拷贝构造函数、析构函数，并在main()中测试该类）

3，一圆形游泳池如图所示，现在需在其周围建一圆形过道，并在其四周围上栅栏。栅栏价格为35元/米，过道造价为20元/平方米。过道宽度为3米，游泳池半径由键盘输入。要求编程计算并输出过道和栅栏的造价。设计一个圆环类来解决该问题。

4，声明并实现一个复数类，使以下代码能够工作
Complex c1(1,2);
Complex c2;
Complex c3(c1);
c3.add(c1);
c3.show();

5，声明并实现一个员工类，内有姓名、性别、年龄、部门、联系电话、工种等信息，实现重载的构造函数、拷贝构造函数、析构函数和相关的接口,并在main()中测试该类。

6，声明并实现一个整型数组类，内部封装一个整型数组(int a[10])，要求能顺序打印数组中的各个元素，统计各个元素之和、均值、最大值、最小值和中值,并在main()中测试该类。

7，创建一个栈类，要求实现构造函数、判断栈是否为空、出栈、入栈等操作，通过数组来间接实现。

8，声明并实现一个矩形类，其属性为矩形的左下角与右上角两个点，并能计算矩形的面积。（先做Point类，再做矩形类，注意组合类的构造函数）

9，创建一个学生寝室类StuRoom，该类能表示寝室名、寝室电话、4个学生。该类中有构造函数、changeRoomName()、changeNum()函数能改变寝室的信息，display()函数把完整的寝室信息打印出来。（学生类有姓名、学号、学院、专业4个属性）

